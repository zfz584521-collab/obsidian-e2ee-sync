# Obsidian 同步插件极简使用手册

目标：让两台电脑之间同步 Obsidian 笔记。

插件现在有两种使用模式：

```text
个人模式：自己填写对象存储 AccessKey，适合个人自用。
商业模式：填写授权服务地址和授权令牌，插件自动获取短期临时凭证。
```

普通用户只需要关心：

```text
第一台：填 5 项，然后点同步。
第二台：粘贴第一台配置，再补 3 项，然后点同步。
```

不要复制：

```text
data.json
```

---


## 一、要复制到另一台电脑的插件文件

只复制这些插件文件：

```text
main.js
manifest.json
styles.css（如果有）
```

不要复制：

```text
data.json
```

插件目录示例：

```text
你的笔记库\.obsidian\plugins\obsidian-sync-plugin\
```

---

## 二、第一台电脑怎么设置

打开 Obsidian 插件设置页：

```text
设置 -> E2EE Sync
```

只填写下面 5 项：

| 要填什么 | 说明 |
|---|---|
| 服务地址 | 对象存储地址，例如阿里云 OSS 地址 |
| 存储桶名称 | bucket 名称 |
| 访问密钥 ID | 对象存储 AccessKey ID |
| 访问密钥密码 | 对象存储 SecretKey |
| 同步密码 | 你自己设置的密码，第二台必须填一样的 |

### 访问密钥 ID、访问密钥密码、同步密码怎么填写

这三个字段不是同一类东西：

| 字段 | 谁提供 | 能不能自己随便写 | 填什么 |
|---|---|---|---|
| 访问密钥 ID | 对象存储服务商 | 不能 | 控制台创建密钥后得到的 Access Key ID |
| 访问密钥密码 | 对象存储服务商 | 不能 | 控制台创建密钥后得到的 Access Key Secret / Secret Access Key / Secret Key |
| 同步密码 | 你自己 | 可以自己设置 | 自己设置一个加密密码，两台电脑必须完全一样 |

访问密钥 ID 和访问密钥密码，需要去你使用的对象存储控制台创建。比如阿里云 OSS、腾讯云 COS、七牛云、MinIO、Cloudflare R2 等，都会有类似“访问密钥”“AccessKey”“API 密钥”的入口。

创建后通常会得到两段内容：

```text
Access Key ID
Access Key Secret
```

插件里这样填写：

```text
访问密钥 ID = Access Key ID
访问密钥密码 = Access Key Secret
```

不同服务商叫法可能不一样，常见叫法如下：

```text
Access Key ID
AccessKey ID
Secret Access Key
Access Key Secret
Secret Key
```

同步密码不是服务商给的，是你自己设置的。它用于加密你的同步数据。

同步密码要求：

```text
第一台和第二台必须完全一样。
设置后要自己记住。
忘记后，远端已经加密的数据无法直接解密。
```

同步密码建议：

```text
长度尽量长一点。
不要用 123456、password、生日、手机号。
可以用一串自己记得住的英文、数字和符号组合。
```

示例：

```text
my-obsidian-sync-2026-very-safe
```

第二台电脑也是同样规则：

```text
访问密钥 ID：填同一套对象存储密钥，或能访问同一个存储桶的密钥。
访问密钥密码：填上面密钥对应的密码。
同步密码：必须填第一台同一个同步密码。
```

然后按顺序点：

```text
检查配置
测试连接
同步
```

如果都成功，第一台就配置好了。

### 商业模式怎么设置

如果你购买或使用的是商业同步服务，不需要填写 AccessKey。

在“使用模式”里选择：

```text
商业模式：授权服务
```

然后只填写：

| 要填什么 | 说明 |
|---|---|
| 授权服务地址 | 服务提供方给你的同步授权地址 |
| 授权令牌 | 服务提供方给你的授权令牌或登录码 |
| 同步密码 | 你自己设置的端到端加密密码，所有设备必须一样 |

商业模式下，下面这些会由授权服务自动下发：

```text
对象存储服务地址
存储桶名称
同步通道前缀
仓库 ID
临时访问凭证
```

注意：

```text
同步密码仍然只保存在你的设备上。
服务提供方不需要知道同步密码，也无法用它直接解密你的笔记。
```

---

## 三、第二台电脑怎么设置

第二台也先安装插件文件：

```text
main.js
manifest.json
styles.css（如果有）
```

不要复制：

```text
data.json
```

### 第一步：在第一台复制配置

在第一台插件设置页点：

```text
复制第二台配置
```

这个按钮会复制必要的同步身份信息。

它不会复制：

```text
访问密钥
访问密钥密码
同步密码
设备 ID
```

### 第二步：在第二台粘贴配置

在第二台插件设置页点：

```text
粘贴配置
```

粘贴后，第二台只需要再补 3 项：

| 要补什么 | 怎么填 |
|---|---|
| 访问密钥 ID | 和第一台一样，或能访问同一个存储桶的密钥 |
| 访问密钥密码 | 和第一台一样，或对应上面的密钥 |
| 同步密码 | 必须和第一台完全一样 |

然后按顺序点：

```text
检查配置
测试连接
同步
```

第二台会从远端下载第一台同步过的笔记。

---

## 四、最简单测试流程

建议先用空测试库，不要一开始用真实大库。

### 1. 第一台上传

第一台新建几个测试文件：

```text
test-a.md
folder/test-b.md
assets/test.txt
```

第一台点击：

```text
同步
```

### 2. 第二台下载

第二台点击：

```text
同步
```

预期：

```text
第二台出现 test-a.md、folder/test-b.md、assets/test.txt。
```

### 3. 第二台修改，第一台拉回

第二台修改：

```text
test-a.md
```

第二台点击：

```text
同步
```

第一台点击：

```text
同步
```

预期：

```text
第一台能看到第二台的修改。
```

---

## 五、哪些东西必须一样，哪些必须不一样

两台电脑必须一样：

```text
服务地址
存储桶名称
同步密码
同步身份信息（通过“复制第二台配置”和“粘贴配置”自动处理）
```

两台电脑必须不一样：

```text
设备 ID
```

你不用手动填写设备 ID，插件会自动生成。不要复制 `data.json`，就不会把设备 ID 复制过去。

---

## 六、常见问题

### 从 0.1.0 升级到 0.1.1

1. 退出 Obsidian。
2. 备份当前插件目录，尤其不要删除 `.obsidian/plugins/obsidian-sync-plugin/data.json`。
3. 用 0.1.1 安装包中的 `main.js`、`manifest.json`、`styles.css` 和本手册覆盖插件目录中的同名文件。
4. 重新打开 Obsidian，在设置页确认使用模式：
   - 个人模式：原有 AccessKey 配置会继续使用。
   - 商业模式：确认授权服务地址、授权令牌和同步密码仍然存在。
5. 点击“测试连接”，通过后再同步。

### 如何回滚

1. 退出 Obsidian。
2. 用上一版安装包覆盖 `main.js`、`manifest.json` 和 `styles.css`。
3. 保留 `data.json`，不要删除本地笔记。
4. 重新打开 Obsidian 后测试连接。

如果回滚到不支持商业模式的旧版本，需要重新使用个人 AccessKey 模式，或恢复升级前备份的插件配置。

### 第二台没有下载文件

检查：

```text
第一台是否已经点过“同步”
第二台是否点过“粘贴配置”
第二台的访问密钥和访问密钥密码是否正确
第二台的同步密码是否和第一台完全一样
```

### 提示连接失败

检查：

```text
服务地址是否正确
存储桶名称是否正确
访问密钥是否正确
访问密钥密码是否正确
网络是否能访问对象存储
```

### 提示解密失败

最常见原因：

```text
第二台同步密码和第一台不一样。
```

处理：

```text
把两台的同步密码改成完全一样。
```

### 不小心复制了 data.json

建议处理：

```text
关闭 Obsidian
删除第二台插件目录里的 data.json
重新打开 Obsidian
重新配置第二台
```

---

## 七、高级设置什么时候用

普通同步不用管高级设置。

只有这些情况才需要看高级设置：

```text
你要修改设备名称
你要开启自动同步
你要检查旧版远端布局
你要迁移旧版远端布局
你要查看同步日志
```

如果只是两台电脑同步笔记，不需要理解仓库 ID、设备 ID、同步通道前缀。

---

## 八、一句话版本

第一台：

```text
填服务地址、存储桶、访问密钥、访问密钥密码、同步密码 -> 测试连接 -> 同步 -> 复制第二台配置
```

第二台：

```text
粘贴配置 -> 填访问密钥、访问密钥密码、同一个同步密码 -> 测试连接 -> 同步
```

不要复制：

```text
data.json
```

---

## 九、高级设置完整说明

普通两台电脑同步时，高级设置基本都不用填写。它们主要用于排查问题、迁移旧数据，或者给更复杂的使用场景预留。

最重要的一句话：

```text
正常使用只填前面的 5 项。
高级设置默认不动。
```

### 1. 设备名称

是否必须填写：

```text
不是必须。
```

作用：

```text
只是给当前电脑起一个容易识别的名字，比如“办公电脑”“家里电脑”。
```

什么时候需要改：

```text
你想在日志或设备列表里更容易看出是哪台电脑时，可以改。
```

推荐填写：

```text
第一台：办公电脑
第二台：家里电脑
```

不填写会怎样：

```text
不影响同步。
```

---

### 2. 区域

是否必须填写：

```text
通常不用填。
```

默认值：

```text
auto
```

作用：

```text
对象存储服务所在区域。大多数情况下插件可以用 auto。
```

什么时候需要改：

```text
只有你的对象存储服务商明确要求填写区域时才需要改。
```

举例：

```text
auto
cn-hangzhou
us-east-1
```

普通用户建议：

```text
保持 auto。
```

---

### 3. 同步通道前缀

是否必须填写：

```text
不是必须。
```

默认值：

```text
空
```

作用：

```text
在同一个存储桶里划分不同的同步空间。
```

什么时候需要填写：

```text
你想在同一个存储桶里放多个测试环境、多个用户、多个不同用途的同步仓库时才需要。
```

举例：

```text
test
personal-notes
work-notes
```

普通两台电脑同步是否需要：

```text
不需要，留空即可。
```

注意：

```text
如果第一台填了同步通道前缀，第二台必须使用同一个前缀。
不过正常情况下你不用手动管它，“复制第二台配置”会一起带过去。
```

---

### 4. 仓库 ID

是否必须手动填写：

```text
不需要手动填写。
```

谁来生成：

```text
插件自动生成。
```

作用：

```text
标识同一个远端同步仓库。两台电脑要同步同一批笔记，就必须使用同一个仓库 ID。
```

用户需要理解吗：

```text
正常不用理解。
```

第一台什么时候生成：

```text
首次同步时会自动生成。
点击“复制第二台配置”前，如果还没有仓库 ID，插件也会自动生成。
```

第二台怎么获得：

```text
第二台点击“粘贴配置”时，会从第一台复制的配置里自动获得。
```

什么时候要点“重新创建仓库”：

```text
只有你想彻底换一个新的远端同步空间时才点。
```

误点会怎样：

```text
会换到一个新的远端仓库。
旧远端数据不会删除，但新旧仓库不会自动互通。
```

普通用户建议：

```text
不要手动改仓库 ID。
不要随便点“重新创建仓库”。
```

---

### 5. 自动同步

是否必须开启：

```text
不必须。
```

默认建议：

```text
关闭。
```

作用：

```text
按固定时间间隔自动执行同步。
```

什么时候开启：

```text
你已经完成两台设备测试，并确认上传、下载、冲突、删除都正常后，再考虑开启。
```

测试阶段为什么建议关闭：

```text
手动同步更可控，出了问题更容易判断是哪一步导致的。
```

---

### 6. 自动同步间隔

是否必须填写：

```text
不必须。
```

默认值：

```text
0
```

含义：

```text
0 表示只手动同步。
```

如果开启自动同步，可以填写：

```text
300
```

含义：

```text
每 300 秒同步一次，也就是约 5 分钟一次。
```

普通用户建议：

```text
先保持 0。
```

---

### 7. 检查远端布局

是否平时需要点：

```text
不需要。
```

作用：

```text
检查远端对象存储里是新版布局，还是早期旧版布局。
```

什么时候需要点：

```text
同步提示旧版布局。
同步异常，怀疑远端数据结构不对。
你之前用过旧版本插件。
```

正常新用户是否需要：

```text
不需要。
```

---

### 8. 迁移旧版布局

是否平时需要点：

```text
不需要。
```

作用：

```text
把旧版远端对象复制到新版远端布局。
旧对象不会被删除。
```

什么时候才需要：

```text
只有“检查远端布局”提示检测到旧版布局时才需要。
```

迁移前必须做什么：

```text
备份本地笔记库。
备份对象存储桶，或者至少确认旧对象不要删除。
```

普通新用户建议：

```text
不要点。
```

---

### 9. 同步日志

是否必须看：

```text
不必须。
```

作用：

```text
查看最近同步过程，用于排查问题。
```

什么时候需要看：

```text
同步失败。
连接失败。
文件没有下载。
怀疑冲突或删除行为不符合预期。
```

安全说明：

```text
日志会尽量自动隐藏访问密钥、同步密码、设备 ID 等敏感信息。
但排查问题时仍然不要随便把完整日志发给别人。
```

---

## 十、高级设置总结表

| 高级设置 | 普通用户要不要填 | 默认怎么处理 | 什么时候才用 |
|---|---|---|---|
| 设备名称 | 可填可不填 | 可留空或随便写 | 想区分办公电脑、家里电脑时 |
| 区域 | 不用填 | 默认 auto | 服务商明确要求区域时 |
| 同步通道前缀 | 不用填 | 默认空 | 一个存储桶里要分多个同步空间时 |
| 仓库 ID | 不用填 | 插件自动生成，复制配置时带给第二台 | 需要换新同步空间时 |
| 自动同步 | 不用开 | 默认关闭 | 确认稳定后再开 |
| 自动同步间隔 | 不用填 | 默认 0 | 开启自动同步后再设置 |
| 检查远端布局 | 不用点 | 出问题才检查 | 旧版本迁移或同步异常 |
| 迁移旧版布局 | 不用点 | 不自动迁移 | 检测到旧版布局时 |
| 同步日志 | 不用看 | 自动记录 | 排查问题时 |

---

## 十一、真正需要用户填写的内容

第一台只需要填写：

```text
服务地址
存储桶名称
访问密钥 ID
访问密钥密码
同步密码
```

第二台只需要：

```text
粘贴配置
访问密钥 ID
访问密钥密码
同步密码
```

其他内容都可以不管。

`data.json` 里可能有密钥、同步密码、设备身份。第二台不能直接用第一台的 `data.json`。

---
